# Public absolute URL of the deployed site (no trailing slash).
# Used for og:image / twitter:image tags in index.html.
# The GitHub Actions workflow overrides this automatically from the repo
# name — so renaming the repository fixes the URL on the next push.
VITE_SITE_URL=https://puspagotame1588-create.github.io/Youtube-analyzer
