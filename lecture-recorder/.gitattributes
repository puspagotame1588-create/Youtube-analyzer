# Batch files must keep Windows line endings: cmd.exe mis-parses labels and
# goto targets in a file that uses bare LF.
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Icons are binary; never normalise their line endings.
*.ico binary
